repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Keep this rev in sync with the Ruff constraint in pyproject.toml.
    rev: v0.15.8
    hooks:
      - id: ruff
        args: ["--fix"]
      - id: ruff-format
